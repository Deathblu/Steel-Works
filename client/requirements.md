## Packages
framer-motion | Smooth entry animations and micro-interactions
embla-carousel-react | For the hero product carousel
embla-carousel-autoplay | Auto-play functionality for the carousel
lucide-react | Icons for the UI (already in base, but emphasizing usage)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["var(--font-sans)"],
  display: ["var(--font-display)"],
}
